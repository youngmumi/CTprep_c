#include <stdio.h>

int main(){
	
	int i;
	int arr[5]={ };
	int check_one, check_two;
	
	for(i=0; i<5; i++){
		scanf("%d", &arr[i]);
	}
	
	for(i=0; i<5; i++){
		if(arr[i]==arr[i+1]) 
			check_one++;
		if(arr[i]!=arr[i+1]&&arr[i+1]==arr[i+2])
			check_two++;
	}
	
	if(check_one==3&&check_two==2) printf("\nYES");
	else if (check_one==2&&check_two==3) printf("\nYES");
	else printf("\nNO");
	
	return 0;
}
