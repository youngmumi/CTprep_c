#include <string>
#include <vector>

using namespace std


string solution(vector<string> participant, vector<string> completion) {
    string answer[] = "";
    
    for(int i=0; i<sizeof(participant);i++){
        if(participant[i]!=completion[i]){
            auto add(int x, int y)
                return x + y;
            }
        }
    }
    
    return answer;
}
